🎬 ระบบจัดการรีวิวภาพยนตร์ (Movie Review Web Application)

เว็บแอปพลิเคชันสำหรับจัดการรีวิวภาพยนตร์ พัฒนาด้วย Flask (Python) และ SQLite
รองรับการเพิ่ม แก้ไข ลบ และแสดงข้อมูลภาพยนตร์ พร้อมโปสเตอร์ คะแนน และเวลาในการบันทึกข้อมูล

โปรเจกต์นี้จัดทำขึ้นเพื่อฝึกการพัฒนาเว็บฝั่ง Backend การจัดการฐานข้อมูล และการออกแบบโครงสร้างระบบแบบ CRUD

📌 ภาพรวมของโปรเจกต์

ระบบนี้พัฒนาเพื่อฝึกและแสดงความเข้าใจในหัวข้อดังต่อไปนี้:

การสร้าง Web Application ด้วย Flask

การออกแบบฐานข้อมูลด้วย SQLite

การทำงานแบบ CRUD (Create, Read, Update, Delete)

การจัดการฟอร์มและรับข้อมูลจากผู้ใช้

การใช้ Jinja2 Template Engine

การออกแบบ UI โทน Dark Theme

การจัดการเวลา (Timestamp) ให้ตรงตามเวลาไทย (UTC+7)

🚀 ความสามารถของระบบ (Features)

🎥 เพิ่มรีวิวภาพยนตร์ใหม่

✏ แก้ไขข้อมูลภาพยนตร์

🗑 ลบภาพยนตร์พร้อมปุ่มยืนยัน

⭐ ให้คะแนนภาพยนตร์ 1–5 ดาว

🖼 รองรับการแสดงโปสเตอร์ผ่าน URL

🕒 บันทึกวันเวลาอัตโนมัติ

🌙 ดีไซน์หน้าตาแบบ Dark Mode

💾 ใช้ฐานข้อมูล SQLite แบบ Lightweight

🛠 เทคโนโลยีที่ใช้ (Tech Stack)
ส่วนของระบบ	เทคโนโลยี
Backend	Python + Flask
Database	SQLite3
Frontend	HTML5, CSS3
Template	Jinja2
Version Control	Git
🏗 โครงสร้างการทำงานของระบบ
ผู้ใช้งาน (Browser)
        ↓
Flask Routes (app.py)
        ↓
SQLite Database (movies.db)
        ↓
HTML Templates (Jinja2)

ระบบมีโครงสร้างคล้ายแนวคิด MVC:

Model → ฐานข้อมูล SQLite

View → ไฟล์ HTML

Controller → Flask Routes

📂 โครงสร้างโฟลเดอร์
movie_review_flask/
│
├── app.py              # ไฟล์หลักของโปรแกรม
├── movies.db           # ฐานข้อมูล SQLite
│
├── templates/
│   ├── index.html      # หน้าแสดงรายการภาพยนตร์
│   ├── add.html        # หน้าเพิ่มภาพยนตร์
│   └── edit.html       # หน้าแก้ไขข้อมูล
│
├── static/
│   └── style.css       # ไฟล์ตกแต่งหน้าเว็บ
│
└── README.md
🗄 การออกแบบฐานข้อมูล (Database Design)
ตาราง movies
CREATE TABLE movies (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    name TEXT NOT NULL,
    review TEXT NOT NULL,
    rating INTEGER NOT NULL,
    poster TEXT,
    created_at TEXT DEFAULT (datetime('now', '+7 hours')),
    updated_at TEXT DEFAULT (datetime('now', '+7 hours')),
    user TEXT
);
แนวคิดการออกแบบ

AUTOINCREMENT เพื่อให้ id ไม่ซ้ำ

NOT NULL เพื่อบังคับกรอกข้อมูลสำคัญ

poster เป็นข้อมูลเสริม (ไม่บังคับ)

ใช้

datetime('now', '+7 hours')

เพื่อปรับเวลาให้ตรงกับเวลาไทย (UTC+7)

🧠 อธิบายการทำงานของโค้ดโดยสรุป
🔹 การเพิ่มข้อมูล (CREATE)

รับข้อมูลจากฟอร์ม

บันทึกลงฐานข้อมูล

Redirect กลับหน้าแรก

🔹 การแสดงข้อมูล (READ)

ดึงข้อมูลทั้งหมดจากตาราง movies

ส่งไปยัง index.html

แสดงผลผ่าน Jinja2

🔹 การแก้ไขข้อมูล (UPDATE)

รับค่า id จาก URL

ดึงข้อมูลเดิมมาแสดงในฟอร์ม

เมื่อกด Update จะอัปเดตข้อมูลในฐานข้อมูล

ปรับค่า updated_at อัตโนมัติ

🔹 การลบข้อมูล (DELETE)

ลบข้อมูลตาม id

Commit การเปลี่ยนแปลงในฐานข้อมูล

⚙️ วิธีติดตั้งและรันโปรเจกต์
1️⃣ Clone โปรเจกต์
git clone https://github.com/your-username/your-repo-name.git
cd your-repo-name
2️⃣ สร้าง Virtual Environment
python -m venv venv

เปิดใช้งาน:

Windows

venv\Scripts\activate

Mac / Linux

source venv/bin/activate
3️⃣ ติดตั้งไลบรารี
pip install flask
4️⃣ รันโปรแกรม
python app.py

จากนั้นเปิดเว็บเบราว์เซอร์ที่:

http://127.0.0.1:5000
📈 สิ่งที่ได้เรียนรู้จากโปรเจกต์นี้

การพัฒนา Web Application ด้วย Flask

การออกแบบฐานข้อมูลและใช้ SQL คำสั่งหลัก

การจัดโครงสร้างโปรเจกต์อย่างเป็นระบบ

การจัดการเวลาและ Timezone

การออกแบบ UI ให้ดูทันสมัย

การใช้ Git สำหรับควบคุมเวอร์ชัน

🔮 แนวทางพัฒนาต่อยอดในอนาคต

ระบบสมัครสมาชิก / เข้าสู่ระบบ

ระบบค้นหาและกรองข้อมูล

การแบ่งหน้า (Pagination)

การอัปโหลดรูปภาพแทนการใส่ URL

การพัฒนาเป็น REST API

การ Deploy ขึ้น Cloud (เช่น Render หรือ VPS)

👨‍💻 ผู้พัฒนา

พัฒนาเพื่อการศึกษาและฝึกปฏิบัติด้าน Web Development
ด้วยภาษา Python และ Flask